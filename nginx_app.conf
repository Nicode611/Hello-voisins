location /wss {
    proxy_pass https://hello-voisins-25649417130d.herokuapp.com:8080;  # Remplacez le port par le port sur lequel votre serveur WebSocket interne écoute.
    proxy_http_version 1.1;
    proxy_set_header Upgrade $https_upgrade;
    proxy_set_header Connection "upgrade";
}
